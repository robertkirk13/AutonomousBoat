ssh-keygen -t ed25519 -f ~/.ssh/deploy_key -N "" -q && echo "=== Add this deploy key to GitHub ===" && cat ~/.ssh/deploy_key.pub && echo "=== Press Enter after adding the key ===" && read && echo -e "Host github.com\n  IdentityFile ~/.ssh/deploy_key\n  StrictHostKeyChecking accept-new" >> ~/.ssh/config && sudo apt install -y i2c-tools python3-smbus2 && sudo raspi-config nonint do_i2c 0 && git clone git@github.com:robertkirk13/AutonomousBoat.git ~/AutonomousBoat && echo "=== Done! Reboot to enable I2C: sudo reboot ==="
